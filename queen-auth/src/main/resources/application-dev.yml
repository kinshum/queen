#服务器端口
server:
  port: 8100

#数据源配置
spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: ${queen.datasource.dev.url}
    username: ${queen.datasource.dev.username}
    password: ${queen.datasource.dev.password}
